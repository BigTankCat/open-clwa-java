server:
  port: ${OPENCLAW_GATEWAY_PORT:18890}

spring:
  application:
    name: openclaw-gateway

# Align with Node: health/ready paths
management:
  endpoints:
    web:
      exposure:
        include: []
  endpoint:
    health:
      show-details: when_authorized
